kind = "web"
previewPath = "/"
title = "Auto Parts For Sale"
version = "1.0.0"
id = "artifacts/auto-parts"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18469

[services.development]
run = "pnpm --filter @workspace/auto-parts run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/auto-parts", "run", "build" ]
publicDir = "artifacts/auto-parts/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18469"
BASE_PATH = "/"
